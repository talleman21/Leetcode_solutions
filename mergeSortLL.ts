import  {createList,ILinkedList} from './linkedList'

const mergeLists = (l1:ILinkedList,l2:ILinkedList) => {
  console.log('awesome')
}

const list1 = createList([1,3,5])
const list2 = createList([2,4,6])

mergeLists(list1,list2)